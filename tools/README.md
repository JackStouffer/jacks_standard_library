A set of programs that almost every JSL project will need.

## What's Here

* `embed` - Generate C code to allow you to easily embed text or binary data into your executable. For use during your build process
* `generate_array` - Generate C code for type array containers. For use during your build process
* `generate_hash_map` - Generate C code for type hash map containers. For use during your build process
